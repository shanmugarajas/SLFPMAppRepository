//created on: Jul 29, 2013
//@author: Venky

package SlfPmAppRules

import com.slf.pmapp.models.Resource;
import org.springframework.validation.Errors;
import org.springframework.validation.ValidationUtils;

//list any import classes here.

//declare any global variables here

rule "Name should not be empty"
when
  	Resource( name == "aaa" ) 
then
	System.out.println("Name should not be aaa!" );
	//$errors.addError( $resource, "name", "Name is empty" );
end

rule "Name should be empty"
when
  	Resource( name != "aaa" ) 
then
	System.out.println("Name should be aaa!" );
	//$errors.addError( $resource, "name", "Name is empty" );
end


